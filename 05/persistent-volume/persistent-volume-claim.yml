apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ps-pvc
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: "standard"
  volumeName: ps-vol
  resources:
    requests:
      storage: 10Gi